# Docker

Planned local services:

- PostgreSQL
- Redis
- API
- worker
- web

Local development should eventually use `docker-compose.yml` for infrastructure services and optionally run application code directly on the host for faster iteration.
