{
  "name": "@carbon/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "echo \"Lint config pending\"",
    "test": "echo \"No worker tests yet\"",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@carbon/calculation-engine": "workspace:*",
    "@carbon/types": "workspace:*",
    "bullmq": "^5.43.1",
    "ioredis": "^5.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
