{
  "name": "@carbon/calculation-engine",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"Lint config pending\"",
    "test": "echo \"Calculation tests to be added next\"",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@carbon/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
