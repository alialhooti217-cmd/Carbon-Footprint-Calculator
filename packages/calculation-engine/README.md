# Calculation Engine

This package will contain the deterministic engineering logic for:

- fuel combustion emissions
- electricity emissions
- process emissions
- unit normalization and validation
- factor matching and factor version traceability

This package should remain independent from the web framework so it can be tested and reused by the API and worker.
